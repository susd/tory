<div class="row">
  <div class="small-12 columns">
    <h2><%= @site.name %></h2>
  </div>
</div>
<div class="row">
  <div class="medium-10 columns">
    <h4 class="subheader">Last 5 devices</h4>
    <%= render partial: 'devices/list', locals: { devices: @devices } if @devices.any? %>
    <p><%= link_to 'See all →', site_devices_path(@site) %></p>
    
    <table>
      <% @product_counts.each do |name, count| %>
      <tr>
        <td><%= name %></td><td><%= count %></td>
      </tr>
      <% end %>
    </table>
  </div>
  <div class="medium-2 columns">
    <%= render 'sites/nav' %>
  </div>
</div>